2:
- Relevant code can be found in recordLib.cc and recordLib.h

2.3
- 100 attributes * 10 bytes = 1000 bytes per record
- Code snippet:
	Record record;
	for (int i = 0; i < 100; i++) {
		record.push_back("0123456789");
	}
	printf("%d \n", fixed_len_sizeof(&record));
